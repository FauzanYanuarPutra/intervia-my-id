[workspace]
members = [
    "common",
    "marketplace_service",
    "ai_service",
    "identity_service",
    "community_service"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
incremental = false
