'use client';
import { Card, EmptyState, StatusBadge } from 'lajukan-ui';
import type { Contact360Model } from './contact360';
function money(cents:number){return new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Math.max(0,cents)/100)}
export function ContactDetail({model}:{model:Contact360Model}){const u=model.user;return <div className="grid gap-4 xl:grid-cols-2"><Card className="p-5"><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-bold">{u.name}</h2><StatusBadge tone={u.kyc==='Verified'?'success':u.kyc==='Rejected'?'danger':'warning'}>{u.kyc}</StatusBadge><StatusBadge tone={u.risk==='high'?'danger':u.risk==='medium'?'warning':'success'}>{u.risk}</StatusBadge></div><p className="mt-2 text-sm text-[color:var(--color-text-soft)]">{u.handle} · {u.role} · {u.city}</p><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-[color:var(--color-text-soft)]">Transaksi</dt><dd className="font-bold">{u.transactions}</dd></div><div><dt className="text-[color:var(--color-text-soft)]">GMV</dt><dd className="font-bold">{money(u.gmvCents)}</dd></div><div><dt className="text-[color:var(--color-text-soft)]">Risk strike</dt><dd className="font-bold">{u.riskStrikes}</dd></div><div><dt className="text-[color:var(--color-text-soft)]">Manual hold</dt><dd className="font-bold">{u.manualHold?'Aktif':'Tidak'}</dd></div></dl></Card><Card className="p-5"><h3 className="font-bold">Riwayat yang tersedia</h3><div className="mt-4 grid gap-2 text-sm"><p>{model.listings.length} listing</p><p>{model.orders.length} order</p><p>{model.support.length} tiket support</p><p>{model.trust?'Trust profile tersedia':'Trust profile belum tersedia'}</p></div>{!model.listings.length&&!model.orders.length&&!model.support.length&&!model.trust?<EmptyState className="mt-4" title="Belum ada riwayat terkait" description="CRM tidak membuat data pengganti ketika sumber belum tersedia."/>:null}</Card></div>}
