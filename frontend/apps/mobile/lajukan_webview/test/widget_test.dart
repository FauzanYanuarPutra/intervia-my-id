import 'package:flutter_test/flutter_test.dart';
import 'package:lajukan_webview/main.dart';

void main() {
  test('Lajukan app root can be constructed', () {
    expect(const LajukanApp(), isA<LajukanApp>());
  });
}
