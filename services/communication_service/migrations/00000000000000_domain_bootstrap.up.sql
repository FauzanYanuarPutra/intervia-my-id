CREATE TABLE IF NOT EXISTS service_meta(service_name TEXT PRIMARY KEY,schema_version TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()); INSERT INTO service_meta VALUES ('communication_service','domain-bootstrap-v1',NOW(),NOW()) ON CONFLICT(service_name) DO NOTHING;